(define-proc
  main
  ()
  ((result Int))
  ((x Int) (y Int))
  (sequence
    (if
      (< 0 y)
      (while
        (< x 100)
        (assign ((x (+ x y))))))
    (assert
      (or
        (<= y 0)
        (and (< y 0) (<= 100 x))))
    (assign ((result 0)))
    (return)))
