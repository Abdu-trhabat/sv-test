; This file is part of the SV-Benchmarks collection of verification tasks:
; https://github.com/sosy-lab/sv-benchmarks
;
; SPDX-FileCopyrightText: 2025 The SV-Benchmarks Community
;
; SPDX-License-Identifier: Apache-2.0

(set-logic QF_NIA)

(set-option :produce-witnesses true)

(set-info :format-version "1.0")

(define-proc
  main
  ()
  ((result Int))
  ((x Int) (y Int))
  (!
    (sequence
      (assign (x 0))
      (!
        (while
          (< x 99)
          (sequence
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (+ x 1))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (- x 2))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (+ x 2))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (- x 2))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (+ x 2))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (- x 4))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (+ x 4))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (+ x 2))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (- x 4))))
            (if (= (mod y 2) 0) (assign (x (+ x 2))) (assign (x (- x 4))))))
        :tag
        main-while-1)
      (! (sequence) :tag main-check-1))
    :tag
    main))

(annotate-tag main-check-1 :check-true (= (mod x 2) (mod y 2)))

(verify-call main ())

(get-witness)
