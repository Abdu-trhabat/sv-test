(define-proc
  main
  ()
  ((result Int))
  ((x Int) (y Int))
  (sequence
    (assign ((x 0)))
    (while
      (< x 99)
      (if
        (= (mod y 2) 0)
        (assign ((x (+ x 1))))
        (assign ((x (+ x 2))))))
    (assert
      (= (mod x 2) (mod y 2)))))
