; This file is part of the SV-Benchmarks collection of verification tasks:
; https://github.com/sosy-lab/sv-benchmarks
;
; SPDX-FileCopyrightText: 2025 The SV-Benchmarks Community
;
; SPDX-License-Identifier: Apache-2.0

(set-logic QF_LIA)

(set-option :produce-witnesses true)

(set-info :format-version "1.0")

(define-proc
  main
  ()
  ((result Int))
  ((x Int) (y Int) (nondet_bool$1 Int))
  (!
    (sequence
      (if (not (and (= x y) (= y 0))) (sequence (assign (result 0)) (return)))
      (!
        (while
          true
          (sequence
            (if (not (not (= nondet_bool$1 0))) (break))
            (assign (x (+ x 1)))
            (assign (y (+ y 1)))))
        :tag
        main-while-1)
      (! (sequence) :tag main-check-1)
      (assign (result 0))
      (return))
    :tag
    main))

(annotate-tag main-check-1 :check-true (and (= x y) (<= 0 x)))

(verify-call main ())

(get-witness)
